@model ScenarioViewModel

@{
	ViewData["Title"] = "Admin Paneli";
}

<div class="container mt-5">
	<div class="row justify-content-center">
		<div class="col-lg-10">
			<div class="card shadow-sm">
				<div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
					<h3 class="card-title mb-0">Senaryo Yönetimi</h3>
					<i class="bi bi-gear-fill"></i>
				</div>
				<div class="card-body">
					<form method="post" asp-action="RunScenario">

						<div class="alert alert-light border mb-4">
							<div class="form-check form-switch">
								<input class="form-check-input" type="checkbox" asp-for="AllowRentalVehicles"
									id="rentalSwitch">
								<label class="form-check-label fw-bold" for="rentalSwitch">
									Kiralık Araçlara İzin Ver
								</label>
								<small class="d-block text-muted">Aktif edilirse, filo yetersiz kaldığında dışarıdan
									araç kiralanabilir.</small>
							</div>
						</div>

						<div class="table-responsive">
							<table class="table table-hover table-bordered align-middle">
								<thead class="table-light">
									<tr>
										<th style="width: 40%;">İlçe / İstasyon</th>
										<th style="width: 30%;">Kargo Adedi</th>
										<th style="width: 30%;">Toplam Ağırlık (kg)</th>
									</tr>
								</thead>
								<tbody>
									@for (int i = 0; i < Model.Stations.Count; i++)
									{
										<tr>
											<td class="fw-bold text-primary">
												@Model.Stations[i].StationName
												<input type="hidden" asp-for="Stations[i].StationId" />
												<input type="hidden" asp-for="Stations[i].StationName" />
											</td>
											<td>
												<input asp-for="Stations[i].CargoCount" class="form-control" type="number"
													min="0" placeholder="0" />
											</td>
											<td>
												<div class="input-group">
													<input asp-for="Stations[i].TotalWeight" class="form-control"
														type="number" step="0.1" min="0" placeholder="0.0" />
													<span class="input-group-text">kg</span>
												</div>
											</td>
										</tr>
									}
								</tbody>
							</table>
						</div>

						<div class="d-grid gap-2 mt-4">
							<button type="submit" class="btn btn-primary btn-lg">
								<span class="fw-bold">🚀 Senaryoyu Başlat</span>
							</button>
						</div>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>